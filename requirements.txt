#############################################
# Python dependencies for schema generation #
#############################################
# Supports:
#  - data-model-schema-graphql-mdf.py (preferred modern MDF parser)
#  - data-model-schema-graphql.py (legacy lightweight YAML parser)
#
# We pin versions for reproducibility and to align with the vendored
# Bento MDF/Meta libraries at external/ccdi-dcc-model/bento-mdf.

# Core model parsing & validation
bento-mdf==0.11.9
bento-meta==0.2.16      # Explicit pin (pulled in by bento-mdf but pinned for clarity)

# YAML parsing (legacy script & some MDF internals)
PyYAML>=6.0.1,<7.0

# Direct runtime deps surfaced in bento-mdf pyproject (avoid relying on transitive looseness)
Jinja2==3.1.6           # Needed when templating or certain MDF features invoked
jsonschema==4.17.3
pydantic>=2.10.4,<3.0
annotated-types>=0.7.0,<0.8.0

# (Optional) uncomment if extending scripts to fetch remote specs or show progress bars
# requests>=2.28.2,<3.0
# tqdm>=4.64.1,<5.0

# (Optional dev/test) uncomment if adding tests around model parsing
# pytest>=7.2.1,<8.0


